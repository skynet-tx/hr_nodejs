// Generated by CoffeeScript 1.6.3
(function() {
  var _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  App.DeleteDepAlertWindow = (function(_super) {
    __extends(DeleteDepAlertWindow, _super);

    function DeleteDepAlertWindow() {
      _ref = DeleteDepAlertWindow.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    DeleteDepAlertWindow.prototype.template = new EJS({
      url: 'templates/general/modal-delete-tpl.ejs'
    });

    DeleteDepAlertWindow.prototype.events = {
      'click #delete-item': 'deleteRecord'
    };

    DeleteDepAlertWindow.prototype.initialize = function() {
      return this.render();
    };

    DeleteDepAlertWindow.prototype.render = function() {
      var description, name;
      name = this.model.get('name');
      description = this.model.get('description');
      this.$el.html(this.template.render({
        modalTitle: 'Warning',
        modalBody: '<p>Are you sure you want to delete record:</p>' + '<p>Department <strong>' + name + '</strong> </p>' + '<p>Description <strong>' + description + '</strong></p>'
      }));
      return this;
    };

    DeleteDepAlertWindow.prototype.deleteRecord = function() {
      return this.model.destroy({
        error: function() {
          return $('#alert-message').html(alertTpl.render({
            alertMessage: "Server Error. Can't save your data. Try again later."
          }));
        },
        success: function() {
          $('#popup-window').modal('hide');
          return Log('Delet alert window was closed');
        }
      });
    };

    return DeleteDepAlertWindow;

  })(App.PopupWondow);

}).call(this);

/*
//@ sourceMappingURL=delete-deptm-alert-window.map
*/
