// Generated by CoffeeScript 1.6.3
(function() {
  var _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  App.DeleteEmplAlertWindow = (function(_super) {
    __extends(DeleteEmplAlertWindow, _super);

    function DeleteEmplAlertWindow() {
      _ref = DeleteEmplAlertWindow.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    DeleteEmplAlertWindow.prototype.template = new EJS({
      url: 'templates/general/modal-delete-tpl.ejs'
    });

    DeleteEmplAlertWindow.prototype.events = {
      'click #delete-item2': 'deleteRecord'
    };

    DeleteEmplAlertWindow.prototype.initialize = function() {
      return this.render();
    };

    DeleteEmplAlertWindow.prototype.render = function() {
      var department, middle_name, name, pasition, surname;
      name = this.model.get('name');
      surname = this.model.get('surname');
      middle_name = this.model.get('middle_name');
      department = this.model.get('department');
      pasition = this.model.get('pasition');
      this.$el.html(this.template.render({
        modalTitle: 'Warning',
        modalBody: '<p>Are you sure you want to delete record:</p>' + '<p>Name:        <strong>' + name + '</strong> </p>' + '<p>Middle name: <strong>' + middle_name + '</strong> </p>' + '<p>Surname:     <strong>' + surname + '</strong> </p>' + '<p>Department:  <strong>' + department + '</strong> </p>' + '<p>Position:    <strong>' + pasition + '</strong></p>'
      }));
      return this;
    };

    DeleteEmplAlertWindow.prototype.deleteRecord = function() {
      return this.model.destroy({
        error: function() {
          return $('#alert-message').html(alertTpl.render({
            alertMessage: "Server Error. Can't save your data. Try again later."
          }));
        },
        success: function() {
          $('#popup-window').modal('hide');
          return Log('Delet alert window was closed');
        }
      });
    };

    return DeleteEmplAlertWindow;

  })(App.PopupWondow);

}).call(this);

/*
//@ sourceMappingURL=delete-employee-alert-window.map
*/
