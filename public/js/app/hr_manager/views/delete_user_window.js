// Generated by CoffeeScript 1.6.3
(function() {
  var _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  App.DeleteUserWindow = (function(_super) {
    __extends(DeleteUserWindow, _super);

    function DeleteUserWindow() {
      _ref = DeleteUserWindow.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    DeleteUserWindow.prototype.template = new EJS({
      url: 'templates/general/modal-delete-tpl.ejs'
    });

    DeleteUserWindow.prototype.events = {
      'click #delete-item': 'deleteRecord'
    };

    DeleteUserWindow.prototype.initialize = function() {
      return this.render();
    };

    DeleteUserWindow.prototype.render = function() {
      var email, name, position, role;
      name = this.model.get('name');
      email = this.model.get('email');
      role = this.model.get('role');
      position = this.model.get('position');
      this.$el.html(this.template.render({
        modalTitle: 'Warning',
        modalBody: '<p>Are you sure you want to delete <u>' + role + '</u>:</p>' + '<p>User: <strong>' + name + '</strong> </p>' + '<p>Email: <strong>' + email + '</strong></p>' + '<p>Position is - <strong>' + position + '</strong></p>'
      }));
      return this;
    };

    DeleteUserWindow.prototype.deleteRecord = function() {
      return this.model.destroy({
        error: function() {
          return $('#alert-message').html(alertTpl.render({
            alertMessage: "Server Error. Can't save your data. Try again later."
          }));
        },
        success: function() {
          $('#popup-window').modal('hide');
          return Log('Delet alert window was closed');
        }
      });
    };

    return DeleteUserWindow;

  })(App.PopupWondow);

}).call(this);

/*
//@ sourceMappingURL=delete_user_window.map
*/
